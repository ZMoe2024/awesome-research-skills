# 贡献指南

## 一起把好用的科研工具分享出来

如果你做过一个解决科研问题的小工具，或者用过一个值得推荐给同行的项目，欢迎投稿。作者可以自荐，使用者也可以推荐别人的作品。

我们征集科研 AI 技能（Skill）、工具连接服务（MCP）、桌面软件、在线工具、插件、脚本和工作流。文献检索、论文阅读、数据分析、科研绘图、组会汇报、实验与仿真、知识管理等方向都欢迎。

一个自动整理文献的小脚本、一套连接实验软件的服务、一款好用的统计软件，都可以从它解决的具体问题讲起。开源项目和可公开使用的软件均可推荐；有付费、账号或机构权限要求时，请注明。

**[点击推荐／自荐一个工具](https://github.com/ZMoe2024/awesome-research-skills/issues/new?template=add-resource.yml)**。也欢迎补充使用经验、纠正类型与失效链接，或[提出教程选题](https://github.com/ZMoe2024/awesome-research-skills/issues/new?template=tutorial.yml)。

## 提交一个资源

提交「收录资源」Issue，只需填写三项：**项目名称、原项目链接、解决的科研问题**。不熟悉 Git 或不方便提交代码，也可以通过这个表单推荐。

类型、运行环境、费用、使用截图、示例结果、教程和实际体验都可以选填。未实测也可以推荐，请如实说明；不需要为了投稿临时安装工具。

优先收录有明确科研用途、可访问文档和具体使用入口的项目。提供原作者仓库、官方文档或官方软件页面；尽量避免聚合目录、邀请链接和推广短链。不能只写“提升科研效率”，请说明输入是什么、完成什么任务、得到什么结果。

请勿将软件或 MCP 标成 Skill。项目提供两种入口时可用 `Skill + MCP`。新的目录链接应先找到原项目再收录；初始目录中遗留的条目已标注，欢迎补全。

## 投稿之后

我们会核对原项目来源、科研用途和必要使用条件，检查是否与已有条目重复；信息不足时，会在 Issue 中补充讨论。收录后保留项目链接与来源信息，尊重原作者署名和项目许可。

如果你愿意分享一次完整的实践过程，也欢迎参与教程与案例共建：提供样本来源、操作步骤、结果和检查方法，帮助后来的人照着完成一个小任务。

## 提交 PR

1. 科研 Skill/MCP/工具编辑 `data/resources.json`；工作台与延伸工具编辑 `data/supplement.json`，参照相邻条目填写字段。资源目录本身不作为工具提交；应从中找到具体项目后收录。新 ID 使用 `COMM-` 加简短英文标识，例如 `COMM-example-tool`；已有 ID 不变。
2. 选择现有分类和类型，写一句具体的中文用途说明。原目录记录日期不明时填 `null`。
3. 如果修改专题，编辑 `data/topics.json`，通过 ID 引用资源。
4. 使用 Python 3.10 或更高版本运行 `python scripts/build.py`，再运行 `python scripts/build.py --check`。
5. 将数据与生成的 Markdown 一起提交。在 PR 中写明来源、改动原因和是否实测。

README、lists、topics 和 `data/full-catalog.json` 由脚本生成，不直接修改。修改导言或生成布局时编辑 `scripts/build.py`。`data/source-manifest.json` 记录初始来源范围，避免漏项；原始条目失效时优先保留 ID 并说明状态，如需删除或合并，应同时说明如何调整来源核对记录。

## 更正或移除

报告条目名称、链接和原因即可。项目更名优先更新原条目；项目删除、用途不符或长期不可用时，可以说明依据并建议移除。单次网络失败不足以证明项目失效。

请用自己的语言概括用途，不复制整份第三方 Skill、文档或源码。提交的原创内容应允许按本仓库 MIT 许可分发；第三方内容须保留相应权利与来源说明。
